services:

  backend:
    build: ./backend
    ports:
      - "8000:8000"
    volumes:
      - chroma_data:/app/chroma_db

  frontend:
    build: ./frontend
    ports:
      - "8501:8501"
    depends_on:
      - backend
volumes:
  chroma_data: